server:
  port: ${LANDSCAPE_PORT}
spring:
  application:
    name: landscape
management:
  endpoints:
    web:
      exposure:
        include: info, prometheus
      base-path:
      path-mapping:
        prometheus: /metrics
        info: /actuator/info